// let firstName = "ali";
// let lastName = "shahbaz";

// function hello(fName, lName) {
//   return `hello ${fName} ${lName}`;
// }

// console.log(hello(firstName, lastName));

// ========================


// Define two variables to hold the first and last name
let firstName = "ali";
let lastName = "shahbaz";

// Define a function that takes in two parameters and returns a greeting message
function hello(fName, lName) {
  return `hello ${fName} ${lName}`;
}

// Call the hello function and log the output to the console
console.log(hello(firstName, lastName));
